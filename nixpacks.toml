[build]
cmd = "npm ci && npm run build"

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
dependsOn = ["setup"]
cmd = "npm ci"

[phases.build]
dependsOn = ["install"]
cmd = "npm run build"